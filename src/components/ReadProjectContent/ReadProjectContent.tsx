import React from "react";
import { setUrl } from "../../lib/setUrl";
import { ProjectType } from "../../types/project";

const ReadProjectContent: React.FC = () => {
  return (
    <div>
      <div>content</div>
    </div>
  );
};

export default ReadProjectContent;
